# Changesets

Every user-visible change receives a Changeset. Screenshot-a-Day versions all runtime and shared packages together. During `0.x`, fixes are patch releases and features or breaking changes are minor releases.
